# ============================================================
# Server Port Configuration
# ============================================================
server.port=8080

# ============================================================
# Database Configuration (MySQL)
# ============================================================
spring.datasource.url=jdbc:mysql://localhost:3306/pharmai_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ============================================================
# Hibernate & JPA Configuration
# ============================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ============================================================
# Mail Sender Configuration (Gmail SMTP)
# ============================================================
# HƯỚNG DẪN: Thay YOUR_GMAIL@gmail.com và YOUR_APP_PASSWORD bằng thông tin thật
# Tạo App Password tại: https://myaccount.google.com/apppasswords
# (Cần bật Xác minh 2 bước trước)
# ============================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tamp1236@gmail.com
spring.mail.password=fdqv oggu hnco snoq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
spring.mail.properties.mail.debug=false

# ============================================================
# VNPay Sandbox Configuration
# ============================================================
# HƯỚNG DẪN lấy credentials sandbox VNPay:
#   1. Đăng ký tại: https://sandbox.vnpayment.vn/merchant_webapi/merchant.html
#   2. Vào Quản lý Website → lấy "Mã website" (TmnCode) và "Khóa bí mật" (HashSecret)
#   3. Thẻ test: 9704198526191432198 / NGUYEN VAN A / 07/15 / OTP: 123456
# ============================================================
vnpay.tmn-code=LF4EF82L
vnpay.hash-secret=IIBIXVUAUHFYQKZTFULPLJIHBRDVKXLN
vnpay.url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
# Return URL: VNPay redirect trình duyệt user về đây sau khi thanh toán
vnpay.return-url=http://localhost:8080/api/payment/vnpay-return
# IPN URL: VNPay gọi server-to-server để xác nhận giao dịch (cần public IP hoặc ngrok)
vnpay.ipn-url=http://localhost:8080/api/payment/vnpay-ipn
# Frontend URL: Backend redirect user về sau khi xử lý callback
vnpay.frontend-url=http://localhost:5173
